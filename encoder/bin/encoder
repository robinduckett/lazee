#!/usr/bin/env coffee

queue = require('../lib/queue')
dnode = require('dnode')

server = dnode
    process: (job) ->
        queue.process(job)
        
    getJobs: (cb) ->
        jobs = queue.jobs
            
        cb(jobs)
        
server.listen(5001)
